import { PermisoIconPipe } from './permiso-icon.pipe';

describe('PermisoIconPipe', () => {
  it('create an instance', () => {
    const pipe = new PermisoIconPipe();
    expect(pipe).toBeTruthy();
  });
});
